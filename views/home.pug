extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each video in Videos
            +videoBlock({
                id: video.id,
                title: video.title,
                views: video.views,
                videoFile: video.fileUrl
            })